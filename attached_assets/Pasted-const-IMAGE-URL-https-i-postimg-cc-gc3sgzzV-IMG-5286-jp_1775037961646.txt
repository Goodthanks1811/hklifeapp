const IMAGE_URL = "https://i.postimg.cc/gc3sgzzV/IMG-5286.jpg";

let w = new WebView();

let html = `
<!DOCTYPE html>
<html>
<head>
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
<style>

*{
box-sizing:border-box;
-webkit-tap-highlight-color:transparent;
}

html,body{
margin:0;
padding:0;
width:100%;
height:100%;
overflow:hidden;
background:#000;
font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;
}

/* FULLSCREEN PHOTO */

.image{
position:fixed;
inset:0;
width:100%;
height:100%;
object-fit:cover;
object-position:center center;
filter:
brightness(0.62)
contrast(1.06)
saturate(0.92);
transform:scale(1.02);
}

/* RED TONE */

.tint{
position:fixed;
inset:0;
background:
radial-gradient(circle at 50% 42%,
rgba(170,20,20,0.10) 0%,
rgba(80,0,0,0.08) 24%,
rgba(0,0,0,0.34) 62%,
rgba(0,0,0,0.60) 100%);
pointer-events:none;
}

/* FACE FOCUS */

.focus-mask{
position:fixed;
inset:0;
background:
radial-gradient(ellipse at 50% 42%,
rgba(255,255,255,0.035) 0%,
rgba(255,80,80,0.040) 16%,
rgba(255,80,80,0.020) 26%,
rgba(0,0,0,0.00) 36%,
rgba(0,0,0,0.16) 62%,
rgba(0,0,0,0.42) 100%);
pointer-events:none;
}

/* GRID */

.grid{
position:fixed;
inset:0;
pointer-events:none;
opacity:0.14;
background:
linear-gradient(rgba(255,80,80,0.055) 1px, transparent 1px),
linear-gradient(90deg, rgba(255,80,80,0.055) 1px, transparent 1px);
background-size:44px 44px;
}

/* SCAN */

.scan-band{
position:fixed;
left:0;
width:100%;
height:104px;
background:
linear-gradient(
to bottom,
rgba(255,0,0,0.00),
rgba(255,90,90,0.06),
rgba(255,255,255,0.12),
rgba(255,90,90,0.08),
rgba(255,0,0,0.00)
);
mix-blend-mode:screen;
animation:scanMove 3.2s linear infinite;
}

.scan-line{
position:fixed;
left:0;
width:100%;
height:2px;
background:linear-gradient(
90deg,
transparent,
rgba(255,170,170,0.75),
rgba(255,80,80,1),
rgba(255,170,170,0.75),
transparent
);
box-shadow:
0 0 10px rgba(255,80,80,0.65),
0 0 22px rgba(255,80,80,0.24);
animation:lineMove 3.2s linear infinite;
}

/* CORNERS */

.corner{
position:fixed;
width:40px;
height:40px;
border:2px solid rgba(255,92,92,0.92);
filter:drop-shadow(0 0 8px rgba(255,50,50,0.55));
}

.tl{ top:18px; left:18px; border-right:none; border-bottom:none; }
.tr{ top:18px; right:18px; border-left:none; border-bottom:none; }
.bl{ bottom:18px; left:18px; border-right:none; border-top:none; }
.br{ bottom:18px; right:18px; border-left:none; border-top:none; }

/* TOP BAR */

.topbar{
position:fixed;
top:22px;
left:0;
width:100%;
padding:0 28px;
display:flex;
justify-content:space-between;
align-items:center;
color:rgba(255,232,232,0.96);
text-transform:uppercase;
letter-spacing:3px;
font-size:12px;
text-shadow:0 0 12px rgba(255,80,80,0.30);
}

.live-wrap{
display:flex;
align-items:center;
gap:8px;
}

.dot{
width:8px;
height:8px;
border-radius:50%;
background:#ff5757;
box-shadow:0 0 10px rgba(255,80,80,1);
animation:blink 1.2s infinite;
}

/* CENTER STATUS */

.center-status{
position:fixed;
left:50%;
bottom:80px;
transform:translateX(-50%);
text-align:center;
color:rgba(255,234,234,0.95);
text-shadow:0 0 10px rgba(255,80,80,0.22);
}

.center-status .main{
font-size:13px;
letter-spacing:3px;
text-transform:uppercase;
margin-bottom:6px;
}

.center-status .sub{
font-size:10px;
letter-spacing:1.6px;
text-transform:uppercase;
opacity:0.8;
margin-bottom:12px;
}

.percent{
font-family:ui-monospace,Menlo,monospace;
font-size:34px;
color:#fff;
text-shadow:
0 0 10px rgba(255,80,80,0.65),
0 0 24px rgba(255,80,80,0.22);
}

.percent-label{
font-size:10px;
letter-spacing:1.7px;
text-transform:uppercase;
opacity:0.8;
}

/* ANIMATION */

@keyframes scanMove{
0%{top:-120px;}
100%{top:100%;}
}

@keyframes lineMove{
0%{top:-2px;}
100%{top:100%;}
}

@keyframes blink{
0%,100%{opacity:0.35;}
50%{opacity:1;}
}

</style>
</head>
<body>

<img class="image" src="${IMAGE_URL}">

<div class="tint"></div>
<div class="focus-mask"></div>
<div class="grid"></div>
<div class="scan-band"></div>
<div class="scan-line"></div>

<div class="corner tl"></div>
<div class="corner tr"></div>
<div class="corner bl"></div>
<div class="corner br"></div>

<div class="topbar">
<div>Biometric Scan</div>
<div class="live-wrap"><div class="dot"></div><div>Live</div></div>
</div>

<div class="center-status">
<div class="main" id="statusMain">Reading facial geometry</div>
<div class="sub" id="statusSub">Contour depth analysis</div>
<div class="percent" id="percent">00%</div>
<div class="percent-label">Scan Complete</div>
</div>

<script>

const percentEl = document.getElementById("percent");
const mainEl = document.getElementById("statusMain");
const subEl = document.getElementById("statusSub");

const phases = [
["Reading facial geometry","Contour depth analysis"],
["Tracking profile structure","Passive biometric pass"],
["Resolving facial topology","Landmark stability check"],
["Refining identity model","Surface map alignment"],
["Verification complete","Profile lock confirmed"]
];

let percent = 0;

function updatePhase(p){
let i = 0;
if (p >= 20) i = 1;
if (p >= 45) i = 2;
if (p >= 70) i = 3;
if (p >= 92) i = 4;
mainEl.textContent = phases[i][0];
subEl.textContent = phases[i][1];
}

setInterval(()=>{
percent++;
if(percent>100) percent=0;
percentEl.textContent = String(percent).padStart(2,"0")+"%";
updatePhase(percent);
},38);

</script>

</body>
</html>
`;

await w.loadHTML(html)
await w.present()